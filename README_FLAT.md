# Flat Version

All files are placed in the root of this ZIP.

Nested paths were renamed using double underscores.

Example:

src/api/Badges.ts

becomes:

src__api__Badges.ts

Important: this flat layout is easier to upload manually, but imports may need editing because the original source expects folders and alias paths.
